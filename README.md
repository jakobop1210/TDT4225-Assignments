# TDT4225-Assignments

To start MySQL sever run:

``` bash
docker-compose up -d
```

To shut down server run:

``` bash
docker-compose down
```

MySql credentials:

- server: db
- username: root
- password: group86
